# ==================== API配置 ====================
# 第三方大模型API密钥（必填）
# 仅支持阿里云 qwen 模型
API_KEY=your_api_key_here

# API基础URL（阿里云 DashScope OpenAI 兼容接口）
API_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# ==================== 模型配置 ====================
# 文本对话模型（用于普通对话、行程规划等）
# 推荐：qwen3-max（强大且成本较低）
# 其他选项：qwen-max, qwen-turbo 等
TEXT_MODEL_NAME=qwen3-max

# 多模态模型（用于图片识别）
# 推荐：qwen-vl-plus（视觉能力强，性价比高）
# 其他选项：qwen-vl-max 等
MULTIMODAL_MODEL_NAME=qwen-vl-plus

# ==================== 模型参数配置 ====================
# 最大输出token数（建议1500-2000，避免超时）
MAX_TOKENS=1500

# 温度参数（0-1，越高越随机，建议0.85）
TEMPERATURE=0.85

# ==================== 网络与重试配置 ====================
# 文本API超时时间（秒，默认60秒）
TEXT_API_TIMEOUT=60

# 多模态API超时时间（秒，默认90秒）
MULTIMODAL_API_TIMEOUT=90

# 最大重试次数（默认3次）
MAX_RETRIES=3

# ==================== 使用说明 ====================
# 1. 复制本文件为 .env
# 2. 将 your_api_key_here 替换为您的真实 API 密钥
#    （从 https://dashscope.console.aliyun.com 获取）
# 3. 根据网络情况调整超时时间和重试次数
# 4. 保存后运行 python app.py 即可启动应用
# 5. 应用将在 http://localhost:7860 打开
