# ============================================================================
# 配置文件模板
# 复制此文件为 .env 并填入你的配置
# ============================================================================

# ============================================================================
# 🚀 快速开始 - 最小化配置（推荐新手）
# ============================================================================
# 只需配置这 3 项即可运行！

# LLM API Key（必填）
LLM_API_KEY=your_api_key_here

# LLM API Base URL（必填）
LLM_API_BASE=https://api.deepseek.com/v1

# LLM 模型名称（必填）
LLM_MODEL=deepseek-chat

# Embedding 提供商: openai（推荐，稳定）, huggingface（本地，可能有依赖冲突）
EMBEDDING_PROVIDER=openai

# Embedding API Key（使用 openai 时需要，可与 LLM_API_KEY 相同）
# EMBEDDING_API_KEY=your_api_key_here

# ============================================================================
# 📝 LLM 配置详解
# ============================================================================

# 温度参数 (0.0-2.0)，越低越确定，越高越随机
TEMPERATURE=0.1

# 最大输出 tokens（可选）
# MAX_TOKENS=2000

# ----- 常用 API 配置示例 -----

# DeepSeek（推荐，性价比高）
# LLM_API_KEY=your_deepseek_api_key
# LLM_API_BASE=https://api.deepseek.com/v1
# LLM_MODEL=deepseek-chat

# OpenAI 官方
# LLM_API_KEY=sk-your_openai_api_key
# LLM_API_BASE=https://api.openai.com/v1
# LLM_MODEL=gpt-4-turbo-preview

# 通义千问（Qwen）
# LLM_API_KEY=your_qwen_api_key
# LLM_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
# LLM_MODEL=qwen-turbo

# 其他 OpenAI 兼容 API
# LLM_API_KEY=your_custom_api_key
# LLM_API_BASE=https://your-api-endpoint.com/v1
# LLM_MODEL=your-model-name

# ============================================================================
# 🎯 Embedding 模型配置
# ============================================================================

# Embedding 提供商: huggingface（推荐，本地免费）, openai（云端付费）
# EMBEDDING_PROVIDER=huggingface

# ----- HuggingFace 本地模型（推荐）-----
# 中文模型（推荐）
EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5
# 备选模型
# EMBEDDING_MODEL=Alibaba-NLP/gte-Qwen2-1.5B-instruct  # 更大更准确
# EMBEDDING_MODEL=BAAI/bge-base-zh-v1.5                # 中等大小

# Embedding 批处理大小
EMBEDDING_BATCH_SIZE=32

# ----- OpenAI Embedding（云端）-----
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small
# EMBEDDING_DIMENSION=1536

# ============================================================================
# 💾 向量数据库配置
# ============================================================================

# 向量库类型: chroma（轻量级，推荐）, qdrant（可扩展）
VECTOR_STORE=chroma

# ----- Chroma 配置（本地轻量级）-----
# 持久化目录会自动创建
# CHROMA_PERSIST_DIR=./data/vector_store/chroma
# CHROMA_COLLECTION_NAME=academic_papers

# ----- Qdrant 配置（生产级）-----
# QDRANT_URL=http://localhost:6333
# QDRANT_API_KEY=
# QDRANT_COLLECTION_NAME=academic_papers

# ============================================================================
# ⚙️ RAG 配置（核心参数）
# ============================================================================

# 文本分块大小（建议 256-1024）
CHUNK_SIZE=512

# 分块重叠大小（建议 chunk_size 的 10-20%）
CHUNK_OVERLAP=50

# 检索返回的 Top-K 文档数量
SIMILARITY_TOP_K=5

# 相似度阈值，低于此值的结果将被过滤 (0.0-1.0)
SIMILARITY_THRESHOLD=0.7

# 响应生成模式: compact, tree_summarize, refine, simple_summarize
RESPONSE_MODE=compact

# ============================================================================
# 🔧 高级功能配置
# ============================================================================

# ----- Reranking（重排序）-----
# 启用重排序可提高检索精度，但会增加计算时间
ENABLE_RERANKING=false
# RERANKER_MODEL=BAAI/bge-reranker-base
# RERANKER_TOP_N=3

# ----- Web 搜索 -----
# 启用联网搜索功能
ENABLE_WEB_SEARCH=true
# WEB_SEARCH_MAX_RESULTS=5

# ============================================================================
# 🖥️ 系统配置
# ============================================================================

# 日志级别: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# 缓存开关
ENABLE_CACHE=true

# 最大并发工作线程数
MAX_WORKERS=4

# GPU 配置
USE_GPU=true
# GPU_DEVICE=0

# ============================================================================
# 📝 使用说明
# ============================================================================
# 
# 1. 复制此文件为 .env
#    cp .env.example .env
# 
# 2. 填入你的配置（至少配置前 3 项）
#    - LLM_PROVIDER
#    - 对应的 API_KEY
#    - EMBEDDING_PROVIDER
# 
# 3. 测试配置
#    python tests/test_system.py
# 
# 4. 开始使用
#    python main.py
# 
# 配置示例：
#   - DeepSeek: LLM_API_KEY=sk-xxx, LLM_API_BASE=https://api.deepseek.com/v1, LLM_MODEL=deepseek-chat
#   - OpenAI: LLM_API_KEY=sk-xxx, LLM_API_BASE=https://api.openai.com/v1, LLM_MODEL=gpt-4-turbo-preview
#   - Qwen: LLM_API_KEY=sk-xxx, LLM_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1, LLM_MODEL=qwen-turbo
# 
# 详细文档: docs/CONFIG_GUIDE.md
# ============================================================================
